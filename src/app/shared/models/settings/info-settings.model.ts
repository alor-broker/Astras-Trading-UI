import { InstrumentKey } from "../instruments/instrument-key.model";
import { WidgetSettings } from "../widget-settings.model";

export interface InfoSettings extends WidgetSettings, InstrumentKey  {
}
