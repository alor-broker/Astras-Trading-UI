export interface PriceData {
  dayOpen: number,
  prevClose: number
  dayChange: number,
  dayChangePerPrice: number
  lastPrice: number,
  ask: number,
  bid: number,
  high: number,
  low: number
}
