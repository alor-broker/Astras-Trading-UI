export interface Position {
  ticker: string,
  todayChangePercent: number,
  type: string,
  typeName: string,
  name: string,
  lastAmount: number,
  lotCount: number,
  portfolioRatePersent: number
}
